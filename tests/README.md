# Tests

pytest suites for repository-level validation.

- `terraform/` - structure, security, routing, and naming tests
- `rules/` - Suricata rule quality and domain-list tests
- `fixtures/` - sanitized fixture data

TODO (Phase 2+): add test modules. Static tests state their limitations clearly
and do not claim to prove packet-level behavior.
