# Scripts

Operational and validation scripts.

TODO (Phase 6): add the following.

- `bootstrap.sh`
- `validate.sh`
- `test-connectivity.sh`
- `test-routes.sh`
- `test-firewall-rules.sh`
- `generate-test-traffic.py`
- `analyze-firewall-logs.py`
- `estimate-costs.sh`

All shell scripts must start with:

```bash
#!/usr/bin/env bash
set -euo pipefail
```
