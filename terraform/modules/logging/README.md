# Module: logging

Firewall logging to CloudWatch (alert/flow log groups + resource policy) and encrypted S3 archival (public access blocked, versioning, lifecycle, BucketOwnerEnforced, bucket policy for log delivery). Configurable retention.

See variables.tf, main.tf, and outputs.tf for the implementation.
