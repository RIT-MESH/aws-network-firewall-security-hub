# Module: network-firewall

AWS Network Firewall creation across multiple Availability Zones, firewall subnet mappings, protection settings, firewall policy attachment, logging configuration, and optional delete/subchange/policy-change protection (enabled by default in the production example).

TODO (Phase 2+): implement main.tf, ariables.tf, outputs.tf, and ersions.tf (if module-local constraints are needed). No resources are declared in this Phase 1 foundation.
