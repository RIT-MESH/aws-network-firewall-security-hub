# Module: network-firewall

Highly available AWS Network Firewall across the provided firewall subnets (one per AZ), policy attachment, protection flags, and optional logging configuration. Exposes per-AZ endpoint IDs for inspection routing.

See variables.tf, main.tf, and outputs.tf for the implementation.