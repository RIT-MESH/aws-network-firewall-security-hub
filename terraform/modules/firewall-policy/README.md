# Module: firewall-policy

AWS Network Firewall policy with stateful Suricata rule groups (allow/deny/alert/dns), domain ALLOWLIST/DENYLIST groups, a stateless drop group, policy IP-set variables, and STRICT_ORDER with explicit priorities.

See variables.tf, main.tf, and outputs.tf for the implementation.
