# Lab environment

Lower-cost reference tfvars for the centralized AWS Network Firewall platform.
The repository uses a single Terraform root at `terraform/`; select the lab
environment by copying `terraform.tfvars.example` to `terraform/terraform.tfvars`.

See `terraform.tfvars.example`. See `docs/security-decisions.md` for the
single-root decision.
