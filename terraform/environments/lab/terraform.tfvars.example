# Example variable values for the lab environment.
# Copy to terraform.tfvars and adjust. Never commit real terraform.tfvars.

environment             = "lab"
owner                   = "platform-team"
aws_region              = "us-east-1"
availability_zone_count = 2
additional_tags = {
  CostCenter = "lab-security-1234"
}
